# mkt-exchange-common

Shared low-level utilities for `mkt` exchange adapters.

This crate is published as `mkt-exchange-common` and must be released before
adapter crates that depend on a new version of it.
